# Transformation Rule Library

211 KLEE-coverage code-transformation skills, one per rule, generated from `rules/` by `scripts/gen_rule_skills.py`.
Each `<name>/SKILL.md` carries the trigger, transformation, rationale, and a before/after C example (also copied as `before.c`/`after.c`).
Not auto-loaded — read a skill on demand when its trigger matches.

To regenerate: `uv run scripts/gen_rule_skills.py --clean`

## Rules

- [`add-newline-to-output`](add-newline-to-output/SKILL.md) — Use when printf statements output strings without trailing newlines
- [`adjust-array-indexing-base`](adjust-array-indexing-base/SKILL.md) — Use when array is accessed with direct user input as index (0-based indexing)
- [`align-array-indexing-with-input`](align-array-indexing-with-input/SKILL.md) — Use when the program accesses a precomputed table using an offset (e.g., arr[n-1]) that introduces an arithmetic operation between the symbolic input and the index
- [`arithmetic-over-char-iteration`](arithmetic-over-char-iteration/SKILL.md) — Use when loop variables iterate over character values (e.g., ASCII codes) to represent different operations or choices
- [`array-lookup-to-explicit-branches`](array-lookup-to-explicit-branches/SKILL.md) — Use when code uses a loop to search for an input value in an array and then uses the found index for further computation
- [`array-to-scalar-chars`](array-to-scalar-chars/SKILL.md) — Use when code reads a fixed number of characters into an array and only accesses individual elements
- [`array-to-scalar-simplification`](array-to-scalar-simplification/SKILL.md) — Use when code uses an array to store a single character/value that is only accessed at one index
- [`array-to-scalar-vars`](array-to-scalar-vars/SKILL.md) — Use when code reads fixed-size input into an array and only accesses individual elements
- [`avoid-arithmetic-comparisons`](avoid-arithmetic-comparisons/SKILL.md) — Use when code uses arithmetic operations (like subtraction) to check character ranges or ASCII values
- [`avoid-arithmetic-constraints`](avoid-arithmetic-constraints/SKILL.md) — Use when code performs arithmetic operations on symbolic input values
- [`avoid-array-indexing-arithmetic`](avoid-array-indexing-arithmetic/SKILL.md) — Use when input is read into an array and then accessed with arithmetic operations to form values
- [`avoid-branch-driven-counter-resets`](avoid-branch-driven-counter-resets/SKILL.md) — Use when code uses a conditional branch to reset/clamp a counter (e.g., `if(cnt < 0){ fill++; cnt = 0; }`) that introduces an extra control-flow path on every loop iteration
- [`avoid-complex-modulo-conditions`](avoid-complex-modulo-conditions/SKILL.md) — Use when code uses complex arithmetic expressions with modulo operations in conditional statements, especially involving multiple variables
- [`avoid-deep-recursive-accumulation`](avoid-deep-recursive-accumulation/SKILL.md) — Use when the solution computes a result via deep tail-style recursion that mixes arithmetic accumulation with the base case (e.g., recursing through y/x*x*2 + f(y%x,x) until x==0)
- [`avoid-early-return-in-loops`](avoid-early-return-in-loops/SKILL.md) — Use when code uses early return statements inside nested loops to exit upon finding a condition
- [`avoid-explicit-branching`](avoid-explicit-branching/SKILL.md) — Use when code uses explicit if-else statements to handle different input cases
- [`avoid-explicit-conditionals`](avoid-explicit-conditionals/SKILL.md) — Use when code uses explicit if-else branches based on input comparisons
- [`avoid-exponential-computation`](avoid-exponential-computation/SKILL.md) — Use when code computes large values through repeated multiplication in loops (e.g., computing powers)
- [`avoid-float-arithmetic-in-integer-loops`](avoid-float-arithmetic-in-integer-loops/SKILL.md) — Use when integer values are updated using floating-point multiplication (e.g., y = y * 1.01) inside a loop condition that KLEE must symbolically reason about
- [`avoid-integer-constraints`](avoid-integer-constraints/SKILL.md) — Use when code reads an integer with scanf and performs arithmetic/logical operations on it
- [`avoid-intermediate-variables`](avoid-intermediate-variables/SKILL.md) — Use when intermediate variables are used to store simple arithmetic expressions before using them in conditional statements
- [`avoid-iterative-gcd-with-mutated-params`](avoid-iterative-gcd-with-mutated-params/SKILL.md) — Use when GCD/LCM is computed via an iterative loop that mutates the input parameters and reuses them as both state and divisor, creating loop-carried symbolic dependencies
- [`avoid-large-fixed-arrays`](avoid-large-fixed-arrays/SKILL.md) — Use when code uses large fixed-size arrays (e.g., box[55555]) for sieve-based algorithms or marking/filtering operations
- [`avoid-large-static-arrays`](avoid-large-static-arrays/SKILL.md) — Use when code declares large static/global arrays that are accessed with symbolic indices
- [`avoid-loop-based-search`](avoid-loop-based-search/SKILL.md) — Use when code uses a loop to search for a value that satisfies an equation or constraint
- [`avoid-loop-based-sqrt`](avoid-loop-based-sqrt/SKILL.md) — Use when computing square root through iterative loop (while i*i < target)
- [`avoid-loop-dependent-multiplication`](avoid-loop-dependent-multiplication/SKILL.md) — Use when a loop performs repeated multiplication where the loop bound depends on symbolic input
- [`avoid-modulo-in-constraints`](avoid-modulo-in-constraints/SKILL.md) — Use when code uses modulo operations (%) in conditional expressions that involve symbolic variables
- [`avoid-modulo-operations`](avoid-modulo-operations/SKILL.md) — Use when code uses modulo operator (%) to extract digits or parts of a number
- [`avoid-multiplicative-constraints-in-branches`](avoid-multiplicative-constraints-in-branches/SKILL.md) — Use when a branch condition uses multiplication of two symbolic inputs (e.g., s*t < d) creating a nonlinear constraint
- [`avoid-nonlinear-symbolic-constraints`](avoid-nonlinear-symbolic-constraints/SKILL.md) — Use when the condition involves nonlinear arithmetic over symbolic integers (e.g., multiplications like 4*a*b or squaring (c-a-b)*(c-a-b)) that the SMT solver struggles to reason about
- [`avoid-puts-use-printf`](avoid-puts-use-printf/SKILL.md) — Use when using puts() function for string output
- [`avoid-recursive-modular-exponentiation-for-inverses`](avoid-recursive-modular-exponentiation-for-inverses/SKILL.md) — Use when the code computes modular inverses via Fermat's little theorem using a power function with a data-dependent loop (pw(x, mod-2)) called repeatedly inside combinatorial helpers
- [`avoid-redundant-bounds-sentinels`](avoid-redundant-bounds-sentinels/SKILL.md) — Use when the algorithm pads a 2D array with sentinel border values and uses nested initialization loops with per-cell conditionals to set them, creating extra symbolic branches per cell
- [`avoid-redundant-intermediate-assignments`](avoid-redundant-intermediate-assignments/SKILL.md) — Use when code introduces an extra variable mutation (e.g., K-=1) before using the value as an array index, creating additional symbolic state updates
- [`avoid-reusing-input-variables`](avoid-reusing-input-variables/SKILL.md) — Use when input variables are modified and reused for computation instead of using separate variables
- [`avoid-scanf-for-char-input`](avoid-scanf-for-char-input/SKILL.md) — Use when using scanf() to read a single character into an integer variable
- [`avoid-side-effects-in-for-update`](avoid-side-effects-in-for-update/SKILL.md) — Use when a for-loop's update expression uses the comma operator to embed I/O or computation with side effects (e.g., printf with a conditional argument), mixing loop control with branching logic
- [`avoid-side-effects-in-loop-headers`](avoid-side-effects-in-loop-headers/SKILL.md) — Use when loop counters are initialized outside the loop and side-effecting expressions (like printf with branching arguments) are embedded in the for-statement's increment clause
- [`avoid-special-case-early-returns`](avoid-special-case-early-returns/SKILL.md) — Use when the code branches on equality of input parameters and returns early via specialized closed-form computations (e.g., Pow) for degenerate cases
- [`avoid-sprintf-for-digit-extraction`](avoid-sprintf-for-digit-extraction/SKILL.md) — Use when code converts integers to strings via sprintf and compares character codes to extract digits for symbolic constraints
- [`avoid-string-ops-for-single-char`](avoid-string-ops-for-single-char/SKILL.md) — Use when using string operations (scanf with %s, string arrays) to read/process single characters
- [`avoid-structured-scanf-parsing`](avoid-structured-scanf-parsing/SKILL.md) — Use when scanf uses a complex format string with literal delimiters (e.g., '%lld.%lld') to parse composite numeric values into multiple integer fields
- [`bitmask-instead-of-array-flags`](bitmask-instead-of-array-flags/SKILL.md) — Use when code uses an auxiliary array indexed by small values to mark set membership and tests membership inside a loop with early-exit branching
- [`bounded-char-input-with-switch`](bounded-char-input-with-switch/SKILL.md) — Use when code reads a fixed-size string buffer with scanf("%s") and then iterates over the buffer with chained if-statements comparing characters
- [`bounded-loop-with-early-exit`](bounded-loop-with-early-exit/SKILL.md) — Use when code uses unbounded counting loops followed by separate comparisons against a symbolic bound, then computes the answer indirectly from the counter value
- [`bounded-loop-with-inline-check`](bounded-loop-with-inline-check/SKILL.md) — Use when code uses a separate scan loop to skip a prefix and then a post-loop comparison against an unrelated bound to decide the output
- [`branchless-ceiling-division`](branchless-ceiling-division/SKILL.md) — Use when code computes ceiling division using a conditional remainder check (e.g., t = a/c; if (a%c != 0) t++;) creating extra symbolic branches
- [`char-comparison-to-numeric-extraction`](char-comparison-to-numeric-extraction/SKILL.md) — Use when multiple character variables are compared for equality using logical OR conditions
- [`char-to-int-comparison`](char-to-int-comparison/SKILL.md) — Use when character variables are used for comparison operations
- [`char-to-int-conversion`](char-to-int-conversion/SKILL.md) — Use when multiple character inputs are read and compared individually
- [`char-to-string-input`](char-to-string-input/SKILL.md) — Use when reading a single character input using scanf with %c format specifier
- [`check-loop-condition-before-mutation`](check-loop-condition-before-mutation/SKILL.md) — Use when a loop uses an unconditional 'while(1)' with the termination check placed after state mutations and side effects (e.g., increment then test-and-break)
- [`collapse-format-branches`](collapse-format-branches/SKILL.md) — Use when code branches solely to choose between printf format strings (e.g., zero-padding via if/else)
- [`collapse-mutually-exclusive-branches`](collapse-mutually-exclusive-branches/SKILL.md) — Use when code uses multiple independent if-statements to check mutually exclusive values of the same variable, creating extra unnecessary branch points
- [`conditional-to-modulo`](conditional-to-modulo/SKILL.md) — Use when code uses conditional subtraction to wrap values within a range
- [`consolidate-state-into-local-array`](consolidate-state-into-local-array/SKILL.md) — Use when the code uses multiple separate global 2D arrays to represent different components of the same DP state, with updates spread across distinct named arrays
- [`correct-format-specifiers-for-types`](correct-format-specifiers-for-types/SKILL.md) — Use when scanf/printf format specifiers do not match the declared variable types (e.g., using %ld for long long or %d for long long), causing undefined behavior on input parsing
- [`data-driven-loop-bounds`](data-driven-loop-bounds/SKILL.md) — Use when iterating over a fixed-size buffer with a constant bound and skipping null/sentinel positions inside the loop body
- [`early-break-on-flag-set`](early-break-on-flag-set/SKILL.md) — Use when a nested loop continues exhaustively after a success condition sets a flag, with no early exit
- [`early-exit-large-input-ranges`](early-exit-large-input-ranges/SKILL.md) — Use when nested loops iterate over a large symbolic range without an analytic shortcut, forcing SE to unroll many iterations and accumulate complex path constraints
- [`early-exit-large-range-shortcut`](early-exit-large-range-shortcut/SKILL.md) — Use when nested loops iterate over a range [a,b] that may be very large, forcing symbolic execution to unroll many iterations to discover that the answer is trivially determined (e.g., when the range exceeds a modulus).
- [`early-exit-on-search-success`](early-exit-on-search-success/SKILL.md) — Use when a loop searches for a satisfying condition by setting a flag and continuing to iterate over the entire space before printing the result
- [`early-exit-special-cases`](early-exit-special-cases/SKILL.md) — Use when code uses array-based cycle detection or complex termination conditions that depend on symbolic memory state
- [`early-exit-trivial-cases`](early-exit-trivial-cases/SKILL.md) — Use when code processes all inputs through a uniform loop without short-circuiting trivial input sizes (e.g., n==1), forcing KLEE to symbolically execute the full loop and array-indexing logic for degenerate cases
- [`early-feasibility-pruning`](early-feasibility-pruning/SKILL.md) — Use when the code performs an expensive enumeration loop without first checking simple necessary conditions (e.g., divisibility by gcd) that would render the entire search infeasible
- [`eliminate-explicit-branches`](eliminate-explicit-branches/SKILL.md) — Use when code uses explicit if-else statements or conditional branches to handle different input cases
- [`eliminate-nested-loops-with-algebraic-check`](eliminate-nested-loops-with-algebraic-check/SKILL.md) — Use when nested loops exhaustively search for integer solutions to linear equations
- [`eliminate-redundant-branching-on-symbolic-input`](eliminate-redundant-branching-on-symbolic-input/SKILL.md) — Use when code uses if/else to compute a value that can be expressed as a single branchless arithmetic expression (e.g., computing absolute difference via conditional negation)
- [`eliminate-redundant-loops-and-io`](eliminate-redundant-loops-and-io/SKILL.md) — Use when the program contains loops or input reads whose results are not needed for the final output (e.g., reading values into a discarded variable, or iterative computations equivalent to a closed-form expression)
- [`expand-arithmetic-to-branches`](expand-arithmetic-to-branches/SKILL.md) — Use when complex arithmetic expressions combine multiple conditions using bitwise/arithmetic operations
- [`expand-equality-comparison`](expand-equality-comparison/SKILL.md) — Use when code directly compares two symbolic variables for equality (a == b)
- [`expand-ternary-to-if-else`](expand-ternary-to-if-else/SKILL.md) — Use when code uses ternary operator (?:) with complex conditions involving logical operators
- [`expand-ternary-to-if-statement`](expand-ternary-to-if-statement/SKILL.md) — Use when conditional expressions use ternary operators with complex conditions
- [`explicit-base-case-initialization`](explicit-base-case-initialization/SKILL.md) — Use when dynamic programming arrays rely on implicit zero-initialization and compute base cases through general recurrence relations
- [`explicit-block-bodies-for-conditionals`](explicit-block-bodies-for-conditionals/SKILL.md) — Use when if-statements use single-statement bodies without braces and combine increment with the conditional check on one line
- [`explicit-branch-structure-over-arithmetic-mutation`](explicit-branch-structure-over-arithmetic-mutation/SKILL.md) — Use when the code mutates input variables in-place via chained arithmetic (e.g., subtracting, then conditionally zeroing, then subtracting again) before a final aggregate predicate, collapsing distinct logical cases into a single path
- [`explicit-branches-over-data-driven-loops`](explicit-branches-over-data-driven-loops/SKILL.md) — Use when the solution encodes problem logic via a lookup table combined with nested loops that compute results indirectly (e.g., scanning an array for matches and using modular arithmetic to traverse it)
- [`explicit-branches-over-stateful-loop`](explicit-branches-over-stateful-loop/SKILL.md) — Use when the code uses a single loop with mutable state variables (like a direction flag and accumulator) that implicitly encodes multiple distinct cases, causing KLEE to explore many iterations of one path instead of distinct branches
- [`explicit-case-branching`](explicit-case-branching/SKILL.md) — Use when code uses a single arithmetic expression with a sign check to handle multiple logical cases (e.g., edge cases like n==1 and ordering conditions like a>b collapsed into one formula)
- [`explicit-else-if-check`](explicit-else-if-check/SKILL.md) — Use when an if-else statement assumes any non-matching case falls into else branch without explicit condition check
- [`explicit-length-loop-over-fixed-bound`](explicit-length-loop-over-fixed-bound/SKILL.md) — Use when code iterates over a fixed-size buffer using the buffer's full capacity as loop bounds and conditionally skips null bytes inside the loop
- [`explicit-loop-condition`](explicit-loop-condition/SKILL.md) — Use when the loop uses 'while(1)' with an internal break controlled by a derived condition, hiding the termination predicate from the symbolic executor
- [`explicit-loop-exit-over-implicit-condition`](explicit-loop-exit-over-implicit-condition/SKILL.md) — Use when a while loop uses an implicit truthiness check on a variable that is also modified inside the loop body (e.g., while(a) with a /= 5)
- [`explicit-loop-over-implicit-encoding`](explicit-loop-over-implicit-encoding/SKILL.md) — Use when loop iterations encode choices implicitly through bit manipulation or arithmetic operations on the loop counter
- [`explicit-loop-termination-condition`](explicit-loop-termination-condition/SKILL.md) — Use when a loop's exit condition depends on a complex/aliased expression (e.g., array element compared to a variable in the for-header) that requires symbolic reasoning across many iterations to determine termination
- [`explicit-range-check`](explicit-range-check/SKILL.md) — Use when code uses arithmetic operations on character values to check ranges (e.g., c - 97 >= 0)
- [`explicit-scanf-input-delimiters`](explicit-scanf-input-delimiters/SKILL.md) — Use when scanf reads multiple integers using a single whitespace-flexible format string like "%d%d%d%d", forcing KLEE to model arbitrary whitespace between tokens
- [`extract-expressions-to-named-variables`](extract-expressions-to-named-variables/SKILL.md) — Use when arithmetic expressions (e.g., a/b, a%b, type-casted divisions) are computed inline inside output or other consuming statements
- [`extract-helper-and-merge-input`](extract-helper-and-merge-input/SKILL.md) — Use when code uses inline conditional negation (e.g., `if(X<0)X=-X;`) and multiple sequential scanf calls, creating extra branch points and fragmented input symbolic regions
- [`factor-compound-arithmetic-into-named-intermediates`](factor-compound-arithmetic-into-named-intermediates/SKILL.md) — Use when the code computes complex compound arithmetic expressions inline (e.g., repeated subexpressions like (s-1)/1000000000+1 used multiple times) directly inside output or branching statements
- [`fixed-to-dynamic-loop`](fixed-to-dynamic-loop/SKILL.md) — Use when a loop has a fixed iteration count that processes variable-length data (e.g., digits of a number)
- [`fixed-to-dynamic-loops`](fixed-to-dynamic-loops/SKILL.md) — Use when a loop has a fixed iteration count that processes variable-length data (e.g., digits of a number)
- [`flatten-nested-conditionals-with-explicit-branches`](flatten-nested-conditionals-with-explicit-branches/SKILL.md) — Use when code uses early-exit continue/break patterns combined with min() helpers and in-place arithmetic updates that implicitly couple constraints across iterations
- [`flatten-struct-to-array`](flatten-struct-to-array/SKILL.md) — Use when state is stored in arrays of multi-field structs with assignments and modular arithmetic intermixed across cells
- [`hoist-invariant-multiplication-out-of-loop`](hoist-invariant-multiplication-out-of-loop/SKILL.md) — Use when a loop accumulates a value that is invariant with respect to the loop's symbolic input (e.g., multiplying a constant factor every iteration regardless of the read value)
- [`hoist-locals-to-globals`](hoist-locals-to-globals/SKILL.md) — Use when intermediate computation results are stored in local variables inside main, embedded directly in expressions, or computed inline within printf arguments
- [`hoist-loop-condition-to-while`](hoist-loop-condition-to-while/SKILL.md) — Use when the main processing loop uses an infinite while(1) with an internal break on a sentinel input value, mixing input reading and termination logic inside the loop body
- [`hoist-modulo-into-loop-state`](hoist-modulo-into-loop-state/SKILL.md) — Use when a loop iterates a candidate divisor and tests divisibility via a modulo on the original input each iteration (e.g., m % i == 0 inside a decrementing loop)
- [`hoist-recursion-state-to-globals`](hoist-recursion-state-to-globals/SKILL.md) — Use when recursive functions pass large mutable buffers and bounds via pointer/parameter chains, creating deep symbolic state that KLEE must track per call frame
- [`hoist-shared-subexpression-and-simplify-branches`](hoist-shared-subexpression-and-simplify-branches/SKILL.md) — Use when the same arithmetic subexpression (e.g., X/D, X%D) is recomputed across branches and intermediate state (like K) is mutated before being used in further conditions
- [`inline-arithmetic-avoid-mutating-operands`](inline-arithmetic-avoid-mutating-operands/SKILL.md) — Use when input variables are mutated in place via compound assignments before being used in a later expression, creating intermediate symbolic state
- [`inline-conditional-computation`](inline-conditional-computation/SKILL.md) — Use when conditional logic modifies pre-computed array values in a separate loop after initial computation
- [`inline-intermediate-arithmetic`](inline-intermediate-arithmetic/SKILL.md) — Use when intermediate arithmetic results are stored in extra named variables that are only used once in a subsequent expression
- [`input-dependent-loop-bounds`](input-dependent-loop-bounds/SKILL.md) — Use when loops have fixed bounds that are independent of symbolic input values
- [`linearize-multiplicative-constraint`](linearize-multiplicative-constraint/SKILL.md) — Use when a branch condition uses multiplication of symbolic inputs (e.g., a <= b*c), creating a nonlinear constraint
- [`loop-counter-to-shift-expression`](loop-counter-to-shift-expression/SKILL.md) — Use when a loop uses multiplication/division by powers of 2 with a counter that starts at 1
- [`loop-on-input-read`](loop-on-input-read/SKILL.md) — Use when main reads input only once via scanf and exits, limiting symbolic exploration to a single input vector per execution
- [`loop-on-string-sentinel`](loop-on-string-sentinel/SKILL.md) — Use when iterating over an input string using a separately-read length variable, creating a coupling constraint between the integer input and string length
- [`loop-on-string-terminator`](loop-on-string-terminator/SKILL.md) — Use when a loop iterates using a separately-read length variable (N) instead of the string's actual null terminator, decoupling control flow from the symbolic string contents
- [`loop-to-closed-form`](loop-to-closed-form/SKILL.md) — Use when iterative loops compute mathematical sequences or powers that have closed-form solutions
- [`loop-to-explicit-cases`](loop-to-explicit-cases/SKILL.md) — Use when nested loops with arithmetic constraints are used to check if an equation has integer solutions
- [`materialize-inputs-into-arrays`](materialize-inputs-into-arrays/SKILL.md) — Use when code processes inputs in a single streaming pass with intertwined conditional state updates, collapsing many input combinations into the same execution path
- [`materialize-stream-input-into-array`](materialize-stream-input-into-array/SKILL.md) — Use when inputs are read and processed in a single online loop, interleaving scanf calls with branching logic on each value
- [`merge-branch-to-conditional`](merge-branch-to-conditional/SKILL.md) — Use when if-else branches contain only simple print statements with constant strings
- [`merge-conditional-branches`](merge-conditional-branches/SKILL.md) — Use when multiple branches lead to the same output/action through separate code paths
- [`merge-nested-branches-into-combined-condition`](merge-nested-branches-into-combined-condition/SKILL.md) — Use when code uses nested if/else branches with separate conditions on the same variables, creating multiple independent path constraints that fragment the symbolic execution tree
- [`merge-redundant-branches`](merge-redundant-branches/SKILL.md) — Use when multiple sequential if/else-if branches produce overlapping or equivalent outputs based on related conditions over the same variables
- [`modulo-to-explicit-checks`](modulo-to-explicit-checks/SKILL.md) — Use when code uses modulo operations in conditional expressions
- [`narrow-integer-types-for-loop-bounds`](narrow-integer-types-for-loop-bounds/SKILL.md) — Use when loop bounds and array indices are declared as long long (64-bit) symbolic inputs, forcing KLEE to reason about wide bitvector constraints for every loop iteration and index computation
- [`nested-loop-early-exit`](nested-loop-early-exit/SKILL.md) — Use when nested loops have conditions that depend on computed values without explicit early termination
- [`nested-loop-to-modular-arithmetic`](nested-loop-to-modular-arithmetic/SKILL.md) — Use when nested loops exhaustively search for integer solutions to linear equations
- [`nested-loop-to-single-loop-division`](nested-loop-to-single-loop-division/SKILL.md) — Use when nested loops are used to find factor pairs by testing all combinations
- [`nested-loop-to-single-loop-modulo`](nested-loop-to-single-loop-modulo/SKILL.md) — Use when nested loops check if a linear combination of loop variables equals a target value
- [`nested-ternary-to-if-else`](nested-ternary-to-if-else/SKILL.md) — Use when nested ternary operators are used for multi-way conditional branching
- [`normalize-array-index-range`](normalize-array-index-range/SKILL.md) — Use when code indexes a counting array using raw character values (e.g., cnt[w[i]]) and iterates over a wide ASCII range like 'a' to 'z'
- [`normalize-boundary-conditions-to-canonical-form`](normalize-boundary-conditions-to-canonical-form/SKILL.md) — Use when boundary checks are written as a disjunction of violation conditions (e.g., x+r>W || x-r<0) using strict inequalities and mixed operand orderings
- [`optimize-prime-checking-loop`](optimize-prime-checking-loop/SKILL.md) — Use when checking primality by iterating from n/2 down to 2 and testing all divisors
- [`per-character-symbolic-input`](per-character-symbolic-input/SKILL.md) — Use when code reads an entire string buffer with scanf("%s") then iterates over it, coupling all character constraints to a single symbolic array
- [`positive-character-class-checks`](positive-character-class-checks/SKILL.md) — Use when code validates input characters by checking only for a single forbidden value (e.g., s[i]=='-') and ORs a flag, leaving the symbolic character largely unconstrained
- [`precompute-recursion-to-table`](precompute-recursion-to-table/SKILL.md) — Use when the program computes values via deep/branching recursion (e.g., tribonacci-like fn with multiple recursive calls and base-case branches) invoked per symbolic input
- [`precompute-table-to-input-driven-loop`](precompute-table-to-input-driven-loop/SKILL.md) — Use when the program precomputes all answers into a static lookup table at startup and then services each input via a single array index, leaving the input variable involved in only a trivial division/index operation
- [`prefer-addition-over-subtraction-in-parity-checks`](prefer-addition-over-subtraction-in-parity-checks/SKILL.md) — Use when checking parity of a combination of symbolic integers using subtraction (e.g., (a - b) % 2) which can produce negative intermediate values and implementation-defined modulo results
- [`prefer-bitshift-over-multiplication`](prefer-bitshift-over-multiplication/SKILL.md) — Use when a loop doubles a symbolic integer using arithmetic multiplication (e.g., X *= 2) as the loop progression operator
- [`prefer-char-by-char-input-over-string-buffer`](prefer-char-by-char-input-over-string-buffer/SKILL.md) — Use when input is read via scanf("%s", buf) into a fixed-size array and then iterated until the null terminator
- [`prefer-counted-for-loop-over-decrement-while`](prefer-counted-for-loop-over-decrement-while/SKILL.md) — Use when the code consumes N symbolic inputs using a 'while(N--)' loop that mutates the loop counter, coupling termination to the symbolic input count variable.
- [`prefer-decrementing-while-over-for-counter`](prefer-decrementing-while-over-for-counter/SKILL.md) — Use when a fixed-iteration loop uses a for-statement with an incrementing counter compared against a constant upper bound (e.g., for(i=0;i<N;i++))
- [`prefer-enumeration-over-conditional-construction`](prefer-enumeration-over-conditional-construction/SKILL.md) — Use when code constructs an answer through nested conditional branches that depend on accumulated symbolic state (flags, per-index sentinels), creating many interacting path constraints
- [`prefer-fixed-bounded-loops-over-conditional-accumulation`](prefer-fixed-bounded-loops-over-conditional-accumulation/SKILL.md) — Use when the code uses a single loop with an index-based conditional (e.g., if i==0 initialize, else accumulate) and branches inside the loop on operand ordering before invoking a helper
- [`prefer-implicit-nonzero-test-over-equality`](prefer-implicit-nonzero-test-over-equality/SKILL.md) — Use when the code branches on an arithmetic equality like (expr % m == 0), forcing KLEE to solve an explicit equality constraint and structuring branches around the rarer (zero) case first
- [`prefer-int-over-long-long-and-reduce-loop-bounds`](prefer-int-over-long-long-and-reduce-loop-bounds/SKILL.md) — Use when the program uses wide integer types (long long) and large fixed loop bounds (e.g. 10000 iterations) for a convergence/termination check
- [`prefer-integer-arithmetic-over-float`](prefer-integer-arithmetic-over-float/SKILL.md) — Use when loop progression depends on multiplying a value by a floating-point constant (e.g., y *= 1.01) with an implicit or no-op cast back to integer
- [`prefer-local-variables-over-globals`](prefer-local-variables-over-globals/SKILL.md) — Use when input variables are declared as globals, KLEE tracks them in persistent memory state, increasing the symbolic state size and constraint complexity across the program.
- [`prefer-narrow-int-and-library-calls-over-nonlinear-arithmetic`](prefer-narrow-int-and-library-calls-over-nonlinear-arithmetic/SKILL.md) — Use when the code branches on a condition built from nonlinear integer arithmetic (e.g., products like 4*a*b and squared expressions over 64-bit symbolic variables)
- [`prefer-parameters-over-globals-in-recursion`](prefer-parameters-over-globals-in-recursion/SKILL.md) — Use when recursive functions communicate state through mutable global variables (e.g., accumulators, counters) that are modified and restored across recursion levels
- [`prefer-sentinel-loop-over-length-counter`](prefer-sentinel-loop-over-length-counter/SKILL.md) — Use when a loop iterates over a string using a separately-read length variable as the bound
- [`prefer-ternary-accumulation-over-if-guarded-add`](prefer-ternary-accumulation-over-if-guarded-add/SKILL.md) — Use when the accumulator is updated only inside an if-statement that branches on a comparison between two array elements
- [`prune-search-space-with-early-termination`](prune-search-space-with-early-termination/SKILL.md) — Use when recursion enumerates a fixed-size combinatorial space (e.g., always iterating to depth 10) and only checks the target condition at the leaves, causing exponential path explosion regardless of input
- [`recursion-to-iteration`](recursion-to-iteration/SKILL.md) — Use when recursive functions are used for computations that can be expressed iteratively
- [`remove-input-loop`](remove-input-loop/SKILL.md) — Use when code uses a while loop with scanf to read multiple inputs until EOF
- [`reorder-disjunctive-conditions`](reorder-disjunctive-conditions/SKILL.md) — Use when multiple conditions are OR'd together and complex expressions appear before simpler ones
- [`replace-array-tracking-with-explicit-termination`](replace-array-tracking-with-explicit-termination/SKILL.md) — Use when code uses arrays to track visited states in a sequence/iteration
- [`replace-array-with-stream-processing`](replace-array-with-stream-processing/SKILL.md) — Use when code uses fixed-size arrays with scanf to read input that is then processed sequentially
- [`replace-bounded-loop-with-fixpoint-exit`](replace-bounded-loop-with-fixpoint-exit/SKILL.md) — Use when code uses an arbitrary large iteration cap (e.g., for t<10000) to bound a loop that should instead terminate on a semantic fixpoint or invariant condition
- [`replace-bounded-search-with-case-analysis`](replace-bounded-search-with-case-analysis/SKILL.md) — Use when code uses nested loops that iterate over a search space derived from input values to find an optimum, causing KLEE to fork on every loop iteration and accumulate path conditions over many symbolic comparisons
- [`replace-data-dependent-loop-exit-with-explicit-state-check`](replace-data-dependent-loop-exit-with-explicit-state-check/SKILL.md) — Use when loop termination depends on a single data-dependent condition (e.g., a counter equaling a target) reached via accumulated mutations, KLEE must symbolically simulate many iterations before any exit branch becomes feasible.
- [`replace-discrete-conditional-chain-with-lookup-table`](replace-discrete-conditional-chain-with-lookup-table/SKILL.md) — Use when code maps a small set of discrete input values to constants via a chain of if/else-if comparisons
- [`replace-fixed-array-with-stream-processing`](replace-fixed-array-with-stream-processing/SKILL.md) — Use when code uses fixed-size arrays with scanf to read input strings
- [`replace-floating-point-with-integer-logic`](replace-floating-point-with-integer-logic/SKILL.md) — Use when using floating-point operations (pow, log10) for integer computations
- [`replace-iterative-countdown-with-closed-form`](replace-iterative-countdown-with-closed-form/SKILL.md) — Use when the code uses a loop that repeatedly subtracts fixed amounts from symbolic values until a condition is met, creating many symbolic branch iterations
- [`replace-iterative-counting-with-closed-form`](replace-iterative-counting-with-closed-form/SKILL.md) — Use when code computes a mathematical quantity (e.g., trailing zeros / factor counts) by iterating over all values up to n with nested loops and modulo checks
- [`replace-iterative-greedy-with-arithmetic`](replace-iterative-greedy-with-arithmetic/SKILL.md) — Use when code uses an iterative loop with cascading if/else-if branches to repeatedly subtract fixed denominations, creating many branch decisions and loop iterations per input.
- [`replace-iterative-loop-with-recursive-function`](replace-iterative-loop-with-recursive-function/SKILL.md) — Use when the code uses an iterative loop with mutable state (e.g., Euclidean algorithm via while-loop with multiple variable updates) to compute a mathematical result
- [`replace-lookup-table-with-explicit-structure`](replace-lookup-table-with-explicit-structure/SKILL.md) — Use when adjacency or transition relations are encoded as opaque numeric lookup tables indexed by symbolic values, forcing KLEE to fork on every table access
- [`replace-loop-search-with-explicit-branches`](replace-loop-search-with-explicit-branches/SKILL.md) — Use when code uses a loop to search for a matching value in an array and stores the index
- [`replace-loops-with-closed-form`](replace-loops-with-closed-form/SKILL.md) — Use when the program uses an iterative loop to compute a value that has a known closed-form arithmetic expression
- [`replace-switch-with-lookup-table`](replace-switch-with-lookup-table/SKILL.md) — Use when code uses a switch/if-else chain to map a small integer key to a constant value, creating multiple distinct control-flow branches
- [`reuse-input-variable`](reuse-input-variable/SKILL.md) — Use when code uses a separate accumulator variable for iterative computations instead of reusing the input variable
- [`scanf-char-to-string`](scanf-char-to-string/SKILL.md) — Use when reading characters one-by-one using scanf("%c") in a loop
- [`scanf-to-getchar`](scanf-to-getchar/SKILL.md) — Use when using scanf() to read single characters
- [`separate-input-from-processing`](separate-input-from-processing/SKILL.md) — Use when input reading is interleaved with conditional logic that updates running aggregates (e.g., tracking max while scanning), creating many symbolic branches inside the input loop
- [`separate-loop-bounds-from-computation`](separate-loop-bounds-from-computation/SKILL.md) — Use when loop bounds and computed values are interdependent within the same loop condition (e.g., j=j*i where j is both the loop variable and the computed value)
- [`simplify-boolean-logic`](simplify-boolean-logic/SKILL.md) — Use when multiple equality comparisons are combined with AND/OR operators to check for specific value combinations
- [`simplify-modulo-branch-condition`](simplify-modulo-branch-condition/SKILL.md) — Use when a branch condition compares a modulo result to zero using an explicit equality (e.g., x%y==0), adding an extra symbolic constraint layer
- [`simplify-modulo-division-chains`](simplify-modulo-division-chains/SKILL.md) — Use when extracting middle digits using nested modulo and division operations like (x%100)/10
- [`simplify-modulo-expressions`](simplify-modulo-expressions/SKILL.md) — Use when extracting middle digits using nested arithmetic operations like (a%100)/10
- [`simplify-multiplicative-constraints`](simplify-multiplicative-constraints/SKILL.md) — Use when multiple equality checks involve multiplication of symbolic variables
- [`simplify-nested-conditionals`](simplify-nested-conditionals/SKILL.md) — Use when code uses nested if-else with compound AND conditions in the if branch
- [`simplify-nested-loop-recurrence`](simplify-nested-loop-recurrence/SKILL.md) — Use when a dynamic programming solution uses a nested loop to compute recurrence relations that sum over multiple previous states
- [`simplify-scanf-format-strings`](simplify-scanf-format-strings/SKILL.md) — Use when scanf uses complex format strings with embedded literal delimiters (e.g., '%lld.%lld') to parse structured numeric input into multiple integer parts
- [`simplify-xor-swap`](simplify-xor-swap/SKILL.md) — Use when code uses XOR swap operations (a^=b^=a^=b) for variable exchange
- [`special-case-small-inputs-early`](special-case-small-inputs-early/SKILL.md) — Use when a single general loop handles all input sizes including degenerate small cases that require complex symbolic constraints to navigate correctly
- [`split-aggregated-checks-into-per-position-branches`](split-aggregated-checks-into-per-position-branches/SKILL.md) — Use when validation is done with a single aggregate counter loop combined with one compound boolean condition over the whole input
- [`split-arithmetic-into-branches`](split-arithmetic-into-branches/SKILL.md) — Use when code uses a single closed-form arithmetic expression (e.g., (s-1)/N+1) to handle boundary cases implicitly within one printf or assignment
- [`split-branches-by-outer-case`](split-branches-by-outer-case/SKILL.md) — Use when a single chain of conditionals encodes multiple symbolic regimes (e.g., relies on a squared-norm comparison that collapses cases like c1r<c2r vs c1r>=c2r into one path) producing few distinct branch points for KLEE to fork on
- [`split-compound-arithmetic-into-branches`](split-compound-arithmetic-into-branches/SKILL.md) — Use when the code collapses a conditional mapping (e.g., wrap-around or modulo with offset) into a single arithmetic expression like (a-1)%N+1
- [`split-compound-branch-conditions`](split-compound-branch-conditions/SKILL.md) — Use when a single conditional chain conflates multiple distinct geometric/logical cases using compound expressions (e.g., comparing against (ar+br)^2 and (ar-br)^2 in one if/else-if ladder), forcing KLEE to solve nonlinear constraints over fewer, more complex path predicates
- [`split-compound-conditions`](split-compound-conditions/SKILL.md) — Use when multiple distinct logical cases are merged into a single compound boolean expression using || or && operators
- [`split-compound-expressions-and-loop-conditions`](split-compound-expressions-and-loop-conditions/SKILL.md) — Use when multiple arithmetic updates are chained via comma operators and assignment expressions on one line, and loop termination is embedded inside a comma-expression scanf condition
- [`split-conjoined-bound-checks`](split-conjoined-bound-checks/SKILL.md) — Use when a single boolean condition combines multiple independent range/bound checks using two-sided inequalities (e.g., r<=x && x<=W-r) joined with &&
- [`split-scanf-calls`](split-scanf-calls/SKILL.md) — Use when a single scanf call mixes numeric format specifiers with a string specifier (%s) into one combined input parsing statement
- [`split-ternary-into-branches`](split-ternary-into-branches/SKILL.md) — Use when the program collapses distinct logical cases into a single nested ternary expression inside a printf/return, producing one straight-line path with combined constraints
- [`split-ternary-into-if-statements`](split-ternary-into-if-statements/SKILL.md) — Use when a single output expression uses a ternary (or compound conditional) to select between distinct behaviors based on a symbolic value
- [`split-ternary-into-separate-ifs`](split-ternary-into-separate-ifs/SKILL.md) — Use when a single output statement uses a ternary/conditional expression on a symbolic value to choose between distinct computations
- [`streaming-getchar-over-buffered-string`](streaming-getchar-over-buffered-string/SKILL.md) — Use when input is read into a fixed-size buffer via scanf/fgets and then scanned with index-based loops over the array
- [`streaming-scan-over-buffered-string`](streaming-scan-over-buffered-string/SKILL.md) — Use when the program reads a whole string with scanf into a fixed buffer and then uses nested loops with array indexing to search for a pattern
- [`ternary-instead-of-if-branch`](ternary-instead-of-if-branch/SKILL.md) — Use when an accumulator is updated only inside an if-statement guarded by a comparison, creating a distinct branch in the control-flow graph
- [`unify-branches-into-single-loop`](unify-branches-into-single-loop/SKILL.md) — Use when the code splits into divergent branches based on input-magnitude heuristics, each running a similar loop with different bounds and update rules
- [`unify-exit-points-via-flag`](unify-exit-points-via-flag/SKILL.md) — Use when the code uses early exit() calls scattered through guard conditions to terminate on different cases
- [`unify-output-paths`](unify-output-paths/SKILL.md) — Use when multiple conditional branches have different printf/output statements with different string literals
- [`unroll-array-loops-to-scalars`](unroll-array-loops-to-scalars/SKILL.md) — Use when fixed-size array inputs are read and processed via loops with index-dependent comparisons and accumulations
- [`use-arithmetic-range-check`](use-arithmetic-range-check/SKILL.md) — Use when code uses direct character comparison against a specific character literal
- [`use-character-literals`](use-character-literals/SKILL.md) — Use when comparing character values using numeric ASCII codes (e.g., 97 for 'a')
- [`use-else-if-chain`](use-else-if-chain/SKILL.md) — Use when multiple independent if statements check the same variable for different values
- [`use-printf-over-puts`](use-printf-over-puts/SKILL.md) — Use when using puts() function for string output
- [`use-strict-inequalities`](use-strict-inequalities/SKILL.md) — Use when code uses less-than-or-equal (<=) or greater-than-or-equal (>=) operators in conditional expressions
- [`widen-accumulator-types-to-prevent-overflow`](widen-accumulator-types-to-prevent-overflow/SKILL.md) — Use when loop accumulators repeatedly multiplied by constants are stored in narrow int types that can overflow for symbolic loop bounds, causing path explosion or undefined-behavior branches in KLEE
- [`wrap-input-in-eof-loop`](wrap-input-in-eof-loop/SKILL.md) — Use when main reads input only once with scanf and exits, limiting symbolic execution to a single input vector per run
- [`wrap-input-in-loop`](wrap-input-in-loop/SKILL.md) — Use when the program reads input once and terminates after processing a single test case
- [`xor-swap-to-explicit-logic`](xor-swap-to-explicit-logic/SKILL.md) — Use when code uses XOR swap operations (a^=b^=a^=b) for conditional transformations
- [`zero-based-array-indexing`](zero-based-array-indexing/SKILL.md) — Use when array is accessed with direct input values as indices without adjusting for zero-based indexing
